package com.github.thebridsk.bridge.data

object ImportStoreConstants {
  /**
   * The file extension, without the ".", for exported bridge stores.
   */
  val importStoreFileExtension = "bridgestore"
}
